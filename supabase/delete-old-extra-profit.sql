-- ============================================================
-- حذف سجلات «الربح الإضافي/الإكرامية» القديمة (نظام الكاش القديم)
-- النظام الجديد بيسجّلها كحركات على حساب الطرف (Advance)، فدي القديمة بس.
-- بحذفها بيختفي تأثيرها من الخزنة والأرباح القديمة.
-- خُذ Backup أولًا. شغّله مرة في Supabase → SQL Editor.
-- ============================================================

-- (اختياري) اعرف العدد قبل الحذف:
-- SELECT "type", count(*), sum("amount") FROM "LedgerEntry"
--   WHERE "type" IN ('EXTRA_PROFIT', 'DRIVER_TIP') GROUP BY "type";

DELETE FROM "LedgerEntry"
WHERE "type" IN ('EXTRA_PROFIT', 'DRIVER_TIP');
